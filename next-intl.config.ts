// next-intl.config.ts
const config = {
  locales: ['en', 'es'],
  defaultLocale: 'en',
  localePrefix: 'as-needed' // optional, use 'always' to force prefix on default locale
}

export default config
